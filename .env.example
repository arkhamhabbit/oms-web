# Copy to .env for local overrides.
#
# VITE_API_BASE_URL — where OMS Core is running. Defaults to http://localhost:8080 (the
# `local` profile's default port) if unset, so most local setups don't need a .env at all.
VITE_API_BASE_URL=http://localhost:8080
